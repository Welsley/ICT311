#pragma once

#include <string>

/**
 * @brief Abstract class all Asset Manager things should inherit from
 * @details This was going to be the class that all other objects inside asset inherits from to make expandability easy 
 * @return N/A
 * 
 * @author Umar Badat
 * @bug Class not actually used due to a concerning due date
 */
class Loaderab
{
public:
	Loaderab(void);
	~Loaderab(void);

	//virtual bool load(std::string location) = 0;
	
};

